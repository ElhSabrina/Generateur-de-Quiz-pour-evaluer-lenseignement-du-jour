//- views/student/result.pug
extends ../layout

block content
  h1 Quiz result – #{quiz.title}

  .card
    if isClosed
      p.small-text Quiz closed by the teacher. Here is your result:
      p.big-score #{correctCount} / #{totalQuestions}
      p.small-text
        | You can now review each question, your answer, and the correct answer.
    else
      p.small-text
        | Your answers are saved, but the teacher has not closed the quiz yet.
        br
        | Once the teacher closes the quiz, refresh this page to see your score and corrections.

  if isClosed
    h2 Corrections
    each a in answers
      .question-block
        h3= a.text
        p
          | Your answer:
          span.badge(class=a.is_correct ? 'badge-success' : 'badge-danger')= a.selected_option
        p
          | Correct answer:
          span.badge.badge-success= a.correct_option
